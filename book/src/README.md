# JunctionRS Book

This directory contains the mdBook documentation. Build with `mdbook build` (after installing mdBook) or serve locally with `mdbook serve`.

Refer to `SUMMARY.md` for chapter ordering.
